server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # SPA routing
  location / {
    try_files $uri $uri/ /index.html;
  }

  # Static assets (optional caching)
  location /assets/ {
    try_files $uri =404;
    add_header Cache-Control "public, max-age=3600";
  }

  # Proxy API to backend container (same user-defined network)
  location ~ ^/(api/|v1/|api/v1/|photos/|api/photos/) {
    resolver 127.0.0.11 ipv6=off;  # Docker DNS
    set $backend_origin http://backend:3000;
    proxy_pass $backend_origin;
    proxy_http_version 1.1;
    proxy_set_header Host              $host;
    proxy_set_header X-Real-IP         $remote_addr;
    proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }

  add_header Cache-Control "no-store";
}
