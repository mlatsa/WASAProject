server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # Single-page app routing
  location / {
    try_files $uri $uri/ /index.html;
  }

  # Avoid overly aggressive caching while testing
  add_header Cache-Control "no-store";
}
