package main

// registerOpenAPIImpl optionally wires OpenAPI routes if your project has them.
// Leaving it empty keeps compatibility where they are not used.
func (r *Router) registerOpenAPIImpl() {
    // no-op (intentionally)
}
