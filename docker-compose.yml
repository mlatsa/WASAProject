version: "3.9"
services:
  backend:
    build:
      context: .
      dockerfile: cmd/server/Dockerfile
    container_name: backend
    networks: [wasa-net]
    ports: ["8080:8080"]   # API → localhost:8080

  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
    container_name: frontend
    networks: [wasa-net]
    depends_on: [backend]
    ports: ["8081:80"]     # Web → localhost:8081

networks:
  wasa-net: {}
