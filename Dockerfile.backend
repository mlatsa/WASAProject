# syntax=docker/dockerfile:1
FROM golang:1.21 AS build
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN go build -o /server ./cmd/webapi

FROM debian:stable-slim
WORKDIR /app
COPY --from=build /server /server
EXPOSE 3000
CMD ["/server"]
